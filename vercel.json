{
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "functions": {
    "api/**.js": {
      "maxDuration": 10
    }
  },
  "git": {
    "deploymentEnabled": true
  },
  "ignoreCommand": "rm -rf node_modules .vercel/output dist && npm install && echo 'Cache cleared!'"
}
